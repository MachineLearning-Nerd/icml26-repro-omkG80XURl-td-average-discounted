# Limitations and deviations — Claim 4

This route verifies the theoretical comparison made in Section 1, not an assertion that finite-run empirical slopes must equal worst-case upper-bound powers. A matched implementation of the older coupled-SA method is reserved as a separate robustness route.
