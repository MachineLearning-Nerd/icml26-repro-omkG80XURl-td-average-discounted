# Limitations and deviations — Claim 5

The dependency route verifies the theorem's stated algebra rather than re-proving every inequality used to obtain its constants. The actual-algorithm family is compatible but non-identifying: it also satisfies a cubic envelope. Monte Carlo uncertainty is recorded from 128 deterministic replicates per condition.
